name: app_saude
description: App de monitoramento de saúde em tempo real

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '^3.3.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^0.13.6
  geolocator: ^10.1.0
  google_maps_flutter: ^2.6.1
  record: ^6.0.0   # 🎤 ÁUDIO FUNCIONANDO
  path_provider: ^2.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true